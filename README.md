# Soltrust

A Trustfall adapter for solidity

## Installation

Install the package

``` shell
cargo install --git https://github.com/hhamud/soltrust
```


Build the binary
``` shell
cargo build --release
```

### Running

Run the Cli
``` shell
soltrust --all
```
